apiVersion: v1
kind: Service
metadata:
  name: userservice
  labels:
    service: userservice
    project: snackbar
spec:
  type: NodePort
  selector:
    service: userservice
    project: snackbar
  ports:
    - port: 8080
      targetPort: 8080

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: userservice
  labels:
    service: userservice
    project: snackbar
spec:
  replicas: 1
  selector:
    matchLabels:
      service: userservice
      project: snackbar
  template:
    metadata:
      labels:
        service: userservice
        project: snackbar
    spec:
      containers:
        - name: userservice
          image: egoomoy/user_service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          # resources:
          #   limits:
          #     memory: "128Mi"
          #     cpu: "100m"
          env:
            - name: SPRING_DATASOURCE_URL
              valueFrom:
                configMapKeyRef:
                  name: spring-info
                  key: db-source
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: mariadb-secret
                  key: username
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mariadb-secret
                  key: password
